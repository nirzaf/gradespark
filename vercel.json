{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "rm -rf node_modules package-lock.json && npm install && npm install three-mesh-bvh@0.8.0 @rollup/rollup-linux-x64-gnu",
        "buildCommand": "npm run build"
      }
    }
  ]
}
